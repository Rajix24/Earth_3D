if u want to see this project in local machine run command in terminal 
NB:IF IT DON'T WORK MAKE SUR THAT U HAVE NPM INSTALL (latest version)
1. npm install --save three
     : "to install three js"
2. npm install --save-dev vite
     : "to install vite"
3. npx vite
     : "to run in ur machine"
